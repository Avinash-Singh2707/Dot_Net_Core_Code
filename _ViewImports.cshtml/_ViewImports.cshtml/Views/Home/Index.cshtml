@model List<Student>       // Binding Student model with index view

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

@{
    foreach (var item in Model)
    {
        <p>@item.Id</p>
        <p>@item.Name</p>
        <p>@item.Gender </p>
    }
}