@* Student-> its namespace is found out in ViewImports *@
@model List<Student>          
@{
    ViewData["Title"] = "Contact";
}

<h1>Contact</h1>

@{
    foreach (var item in Model)
    {
        <p>@item.Id</p>
        <p>@item.Name</p>
        <p>@item.Gender </p>
    }
}

